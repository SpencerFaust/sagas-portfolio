import React, { Component } from 'react';

class Form extends Component {
    // Renders the entire app on the DOM
    render() {
      return (
        <div>
            Form
        </div>
      );
    }
  }
  
  export default Form;